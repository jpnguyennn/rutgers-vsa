import React from "react";

const GalleryItem = ({
	key,
	event_name,
	date,
	insta,
	cover_picture,
	drive,
}: {
	key: number;
	event_name: string;
	date: Date;
	insta: string;
	cover_picture: string;
	drive: string;
}) => {
	return (
		<div>
			<h1>IT WORKS</h1>
		</div>
	);
};

export default GalleryItem;
